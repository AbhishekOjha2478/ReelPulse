requests
python-dotenv

# YouTube
yt-dlp
# yt-dlp plugin that fetches proof-of-origin tokens from the bgutil provider
# server (started as a Docker container in the workflow). PO tokens are what
# let downloads work from GitHub Actions' flagged datacenter IPs.
bgutil-ytdlp-pot-provider
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# Free-tier AI for trend-keyword normalization: called directly over REST
# in app/trends.py (no SDK) since google-generativeai is now fully
# deprecated/unsupported -- avoids being broken by the next SDK swap too.

# Trend sources
pytrends

# Video editing (GitHub Actions ubuntu-latest runners already have ffmpeg preinstalled)
scenedetect

# Storage / static dashboard (no server -- rendered to docs/ for GitHub Pages)
sqlalchemy
jinja2
